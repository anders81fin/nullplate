Build configuration only; the app itself is unchanged from 1.0.3.

The APK no longer carries the Gradle dependency metadata block, which is encrypted for Google Play and rejected by F-Droid. This lets F-Droid verify the reproducible build and ship the release-signed APK.
