Build configuration only; the app itself is unchanged.

Removes the Gradle toolchain resolver plugin, which downloads JDKs from a remote service during the build. F-Droid builds from source on machines that cannot fetch binaries mid-build, and rejected it.
